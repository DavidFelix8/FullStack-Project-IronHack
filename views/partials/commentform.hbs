{{#if user}}

<form action="/{{ post._id }}/post/{{ post._id }}/comment" method="POST">
  <label for="input-content"></label>
  <textarea name="content" id="input-content" placeholder="Your comment here..."></textarea>
  <br>
  <button>Submit Comment</button>
</form>

{{else}}

  {{#each comments}}
    {{>comment}}
  {{/each}}

{{/if}}